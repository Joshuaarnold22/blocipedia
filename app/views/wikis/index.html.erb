
<br>
<div class="jumbotron">
  <div class="container">
    <h1>Blocipedia</h1>
    <p>Blocipedia is a great way to share information!</p>
    <p>
        <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary' %> or
        <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary' %> today!
    </p>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h2>Create a new Wiki topic</h2>
      <p><%= link_to "Create", new_wiki_path, class: 'btn btn-primary' %></p>
    </div>
    <div class="col-md-6">
      <h2>Active Wiki's</h2>
      <% @wikis.each do |wiki| %>
        <p><%= link_to wiki.title, wiki_path(wiki.id) %>
        <small>
          submitted <%= time_ago_in_words(wiki.created_at) %> ago by <%= wiki.user.name %>
        </small></p>
      <% end %>
    </div>
  </div>
</div>
