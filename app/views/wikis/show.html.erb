<div class="container">
  <h1><%= markdown_to_html @wiki.title %></h1>
  <% if policy(@wiki).edit? %>
    <%= link_to "Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>
    <%= link_to "Delete", @wiki, method: :delete, class: 'btn btn-danger' %>
  <% end %>
  <p><%= markdown_to_html @wiki.body %></p>

  <% @wiki.collaborators.each do |collab| %>
    <span><%= collab.user.name %></span>
    <br/>
  <% end %>

  <h3>Add Collaborators </h3>
  <%= form_for @collaborator do |f| %>
    <div class="form-group">
      <%= f.label "email" %>
      <%= text_field_tag "email", nil, class: 'form-control', placeholder: "Collaborator's email" %>
    </div>
    <div class="form-group">
      <%= f.hidden_field :wiki_id, class: 'form-control', placeholder: "Collaborator's email", value: @wiki.id %>
    </div>
    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
  <% end %>


    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter your info" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
</div>
